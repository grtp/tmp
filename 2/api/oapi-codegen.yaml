# oapi-codegen v2 設定。
# 実行: make generate  (go run で実行するためバイナリのインストール不要)
package: api
output: gen/api/api.gen.go
generate:
  models: true
  gin-server: true
  embedded-spec: true
output-options:
  # 生成物の先頭コメントに実行コマンドを残す
  user-templates: {}
