# push ごとに make check(E2E 以外の全検証)を使い捨てコンテナで実行する。
# GitLab 移行時はこのファイルを .gitlab-ci.yml のシムに書き換えるだけ
# (ロジックは全て Makefile 側にある)。
name: check
on: [push]
jobs:
  check:
    runs-on: ftool-ci
    steps:
      - uses: actions/checkout@v4
      - run: cd frontend && npm ci
      - run: make check
